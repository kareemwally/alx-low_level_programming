#include"main.h"
/**
 * mul - Function
 * Description:'This program is to check only number characters'
 *@a:the first num
 *@b:the second num
 * Return:int (multiplication)
 */
int mul(int a, int b)
{
return (a * b);
}
