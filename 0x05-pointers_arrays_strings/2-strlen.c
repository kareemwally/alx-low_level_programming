#include"main.h"
#include <stdlib.h>
/**
 *_strlen - function
 *description: this function returns the length of string
 *return:int (length of string)
 */
int _strlen(char *s)
{
int x = strlen(s);
return (x);
}
